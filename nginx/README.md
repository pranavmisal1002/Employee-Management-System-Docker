Configure Nginx for React Routing

**Create nginx directory:**
Create a directory in the front-end named nginx(ex. frontend/nginx/default.conf). Inside the nginx directory, create a file named default.conf and paste the following content into it.
